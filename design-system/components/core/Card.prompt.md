Card = white, radius 16, 18px padding, near-invisible shadow. CardHeader is the only title treatment used.

```jsx
<Card>
  <CardHeader icon={<Icon name="wallet" size={15} />} title="Account Balance"
    actions={<IconButton label="More" variant="ghost" size={26}><Icon name="more-horizontal" size={16} /></IconButton>} />
  …
</Card>
```
