Icon-only button. `outline` circles sit in the top bar; `ghost` is the card-header "…" overflow.

```jsx
<IconButton label="Notifications"><Icon name="bell" size={16} /></IconButton>
<IconButton label="More" variant="ghost" size={26}><Icon name="more-horizontal" size={16} /></IconButton>
```
