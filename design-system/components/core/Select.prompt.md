Small text-plus-chevron dropdown; it never looks like a form field, it looks like a label you can click.

```jsx
<Select value="USD" options={['USD','EUR','GBP','BDT']} leading={<img src="assets/flag-usd.png" width="16" alt="" />} />
<Select value="This Year" options={['This Year','This Month']} variant="outline" />
```
