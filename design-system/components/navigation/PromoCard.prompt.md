The only saturated block in the sidebar; one per screen, always last.

```jsx
<PromoCard title="Upgrade Pro! 🏆" body="Higher productivity with better organization"
  action={<Button variant="onBrand" size="md" fullWidth icon={<Icon name="crown" size={14} />}>Upgrade</Button>} />
```
